---
extends: default

ignore: |
  node_modules/
  .venv/

rules:
  line-length:
    max: 120
  # GitHub's workflow `on:` key is a YAML boolean to a strict reader and a trigger list
  # to Actions. Actions wins.
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
